<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             mc:Ignorable="d"
             x:Class="QuestHelper.View.MakeNewRoutePage" NavigationPage.HasNavigationBar="False" NavigationPage.HasBackButton="False" Appearing="ContentPage_Appearing" Disappearing="ContentPage_Disappearing" BackgroundColor="White">
    <ContentPage.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="4*" />
                <RowDefinition Height="1*" />
                <RowDefinition Height="5*" />
            </Grid.RowDefinitions>
            <StackLayout Grid.Row="0" Grid.Column="0" Orientation="Vertical" HorizontalOptions="FillAndExpand">
                <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                    <Image Source="baseline_arrow_back_black_48.png" WidthRequest="32" HeightRequest="32" Margin="5,5,0,0" VerticalOptions="Start" HorizontalOptions="Start">
                        <Image.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding BackNavigationCommand}"/>
                        </Image.GestureRecognizers>
                    </Image>
                    <StackLayout HorizontalOptions="FillAndExpand"></StackLayout>
                    <Image Source="delete.png" WidthRequest="32" HeightRequest="32" Margin="0,5,5,0" VerticalOptions="Start" HorizontalOptions="End">
                    </Image>
                </StackLayout>

                <ListView ItemsSource="{Binding RoutePoints}" SeparatorVisibility="None">
                    <ListView.Header>
                        <StackLayout>
                            <Label Text="Начало маршрута" Margin="10,5,10,5"></Label>
                        </StackLayout>
                    </ListView.Header>
                    <ListView.Footer>
                        <StackLayout>
                            <Label Text="Конец маршрута" Margin="10,5,10,5"></Label>
                        </StackLayout>
                    </ListView.Footer>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Margin="0" Orientation="Horizontal" HorizontalOptions="Fill" VerticalOptions="FillAndExpand">
                                    <ffimageloading:CachedImage Source="connected_dot_black_48.png" WidthRequest="48" HeightRequest="48" HorizontalOptions="Start" VerticalOptions="Fill">
                                    </ffimageloading:CachedImage>
                                    <Label Text="{Binding Name}" HorizontalOptions="End" VerticalOptions="Center"/>
                                    <ffimageloading:CachedImage Source="land2.png" WidthRequest="70" HeightRequest="70" HorizontalOptions="Start" VerticalOptions="Fill" Aspect="AspectFit" Margin="2">
                                    </ffimageloading:CachedImage>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>
            </StackLayout>

            <StackLayout Grid.Row="1" Grid.Column="0" Orientation="Horizontal" HorizontalOptions="End">
                <Label VerticalOptions="Center" Text="Все фотографии"/>
                <CheckBox VerticalOptions="Center" IsChecked="{Binding IsShowAllImages}"></CheckBox>
            </StackLayout>
            <CollectionView Grid.Row="2" Grid.Column="0" x:Name="colImages" ItemsSource="{Binding Images}" RemainingItemsThreshold="5" RemainingItemsThresholdReachedCommand="{Binding ImagesTresholdReachedCommand}" IsGrouped="False" >
                <CollectionView.GroupHeaderTemplate>
                    <DataTemplate>
                        <Label Text="{Binding ImageCreateDateString}"></Label>
                    </DataTemplate>
                </CollectionView.GroupHeaderTemplate>
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Span="4" Orientation="Vertical"></GridItemsLayout>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Margin="0">
                            <Frame HasShadow="True" CornerRadius="0" Padding="2" Margin="1">
                                <StackLayout>
                                    <Label Grid.Row="0" Text="{Binding ImageCreateDateString}" FontSize="10"></Label>
                                    <ffimageloading:CachedImage Grid.Row="0" Grid.RowSpan="2" Source="{Binding ImagePath}" DownsampleToViewSize="True" CacheType="All" CacheDuration="86400" BitmapOptimizations="False" Aspect="AspectFill" LoadingPlaceholder="emptyphoto.png" >
                                        <ffimageloading:CachedImage.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding Path=BindingContext.ViewPhotoCommand, Source={x:Reference Name=colImages}}" CommandParameter="{Binding}"/>
                                        </ffimageloading:CachedImage.GestureRecognizers>
                                    </ffimageloading:CachedImage>
                                </StackLayout>
                            </Frame>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>


        </Grid>
    </ContentPage.Content>
</ContentPage>