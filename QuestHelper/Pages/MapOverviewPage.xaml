<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="QuestHelper.MapOverviewPage" Appearing="ContentPage_Appearing">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <maps:Map Grid.Row="0"
                x:Name="MapOverview"
                IsShowingUser="true"
                MapType="Street" 
                />
            <StackLayout Grid.Row="1" Orientation="Horizontal" Padding="5">
                <Button Margin="0" Text="Map" HeightRequest="64" WidthRequest="80" Clicked="Button_Clicked">
                </Button>
                <Button Margin="0" Text="List" HeightRequest="64" WidthRequest="80">
                </Button>
                <Button Margin="0" Text="New" HeightRequest="64" WidthRequest="80">
                </Button>
                <Button Margin="0" Text="Around" HeightRequest="64" WidthRequest="80">
                </Button>
                <Button Margin="0" Text="Profile" HeightRequest="64" WidthRequest="80">
                </Button>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>
